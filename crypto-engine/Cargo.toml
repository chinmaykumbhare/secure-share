[package]
name = "crypto-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

serde = { version = "1", features = ["derive"] }
serde_json = "1"

base64 = "0.22"

rand = "0.8"

getrandom = { version = "0.2", features = ["js"] }